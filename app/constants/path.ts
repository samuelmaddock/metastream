import path from 'path'

export const SOURCE_PATH = __dirname
export const ASSETS_PATH = path.join(SOURCE_PATH, 'assets')
export const BUILTIN_PAGES_PATH = path.join(SOURCE_PATH, 'builtin-pages')
export const RESOURCES_PATH = path.join(SOURCE_PATH, 'dist')
